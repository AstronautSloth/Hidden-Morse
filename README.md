# Hidden-Morse
A sender and receiver pair of scripts to send a message as morse code hidden in the inter-packet timing

Timings could be changed to speed up runtime as well as extending the morse alphabet to use numbers/special characters
